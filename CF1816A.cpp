#include <bits/stdc++.h>
using namespace std;

void solve(){
	int a,b;
	cin>>a>>b;
	cout<<2<<'\n'<<1<<' '<<b-1<<'\n'<<a<<' '<<b<<'\n';
	return;
}

int main(){
	ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
	int t;
	cin>>t;
	while(t--)solve();
}
