#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pll pair<ll,ll>
#define pii pair<int,int>
#define fs first
#define sc second

const int mxn = 35;
const int sh = 11;
int grid[mxn][mxn];

int main(){
	ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
	int na,ma,nb,mb,nx,mx;
	cin>>na>>ma;
	string a[na];
	for(auto &i:a)cin>>i;
	cin>>nb>>mb;
	string b[nb];
	for(auto &i:b)cin>>i;
	cin>>nx>>mx;
	string tar[nx];
	for(auto &i:tar)cin>>i;
}
