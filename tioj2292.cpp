#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int ans = 0;
    char pre = '1';
    int cnt = 0;
    int dp[2][2] = {};
    char c;
    while(cin>>c){
        if(c == '1'){
        }
        else{
        }
    }
    ans += min(cnt,2);
    printf("%d",ans);
}